const logger = output => {
    console.log(output)
}
export const appName = "ES6 Review"; export const dummyFunction = () => {
    let feedback = "I am a dummy function"; //mutable variable feedback = "I am still a dummy function";//value changed return feedback;
}
export default logger;